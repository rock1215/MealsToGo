import styled from "styled-components/native";
import { SafeArea } from "../../../components/utility/safe-area.component";

export const EmptyFavouritesArea = styled(SafeArea)`
  align-items: center;
  justify-content: center;
`;
